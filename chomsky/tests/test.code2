const inteiro max = 20;
var booleano numeros<<max>>;

var real valor;

programa
  inicio
    valor = conta();
    valor = (valor / 5)*10;
    escreva(valor);
  fim

funcao inteiro conta()
  inicio
   var inteiro conta;  
   var real nada;
   conta = 0;

   enquanto (conta < max) faca
     inicio
      escreva (conta+1);
      conta = i*2;
     fim
  fim
